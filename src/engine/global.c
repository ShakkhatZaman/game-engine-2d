#include "global.h"

Renderer_state rendering_state = {NULL, 0, 0};

Input_states keys = {KEY_UNPRESSED, KEY_UNPRESSED, KEY_UNPRESSED, KEY_UNPRESSED, KEY_UNPRESSED};

Config_state config = {0};

Time_state timing = {0, 0, 0, 0, 0, 0, 0, 0};
